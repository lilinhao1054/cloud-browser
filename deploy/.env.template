# Docker Compose 环境变量配置

# browser-manager-server
MAX_BROWSERS=10
# 浏览器超时时间（毫秒），30分钟 = 1800000
BROWSER_TIMEOUT=1800000

# cloud-browser-server
SCREENCAST_QUALITY=60
SCREENCAST_EVERY_NTH_FRAME=3

# cloud-browser-sdk
# 注意：这是构建时变量，SDK 在浏览器中运行，需要填写用户可访问的宿主机地址
VITE_SDK_SERVER_URL=http://localhost:4000
VITE_API_BASE_URL=http://localhost:4000
